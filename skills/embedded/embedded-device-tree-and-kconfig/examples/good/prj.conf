# Correct: enable the ST7789V driver. The symbol only exists when the DT has a
# compatible node (depends on DT_HAS_SITRONIX_ST7789V_ENABLED), so the Kconfig
# choice and the devicetree node must be changed together.
CONFIG_ST7789V=y
