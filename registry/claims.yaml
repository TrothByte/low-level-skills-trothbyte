# Claims / Provenance Registry — traceability нормативных утверждений
# Модель: claim → source → section → skill. Позволяет автоматически проверять provenance.
# Каждое НОРМАТИВНОЕ или критически важное утверждение должно быть traceable.

meta:
  updated: 2026-08-14
  rule: "Нормативное утверждение без source-reference недопустимо в stable skill."
  confidence:
    MUST_VERIFY: "требует исполнимой проверки (см. verification method)"
    MUST_CONSIDER: "необходимо учитывать при reasoning, проверка контекстная"
    OPTIONAL: "справочная информация"
  evidence_levels: ["KNOWN (source-backed)", "INFERRED (выведено из source)", "UNVERIFIED (нуждается в проверке)"]

# ============================================================
# Примеры core claims (расширяется при реализации skills)
# ============================================================
claims:
  - {id: CL-001, claim: "strncpy не гарантирует NUL-терминацию при заполнении буфера целиком", source: "iso-c11-n1570", section: "7.24.2.4", skill: "c-string-and-buffer-safety", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-002, claim: "Сдвиг на отрицательное число бит или на >= ширины типа — UB", source: "iso-c11-n1570", section: "6.5.7", skill: "c-undefined-behavior", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-003, claim: "Signed integer overflow — UB; unsigned — wrap (modulo 2^N)", source: "iso-c11-n1570", section: "6.5 p5, 6.2.5 p9", skill: "c-integer-promotion-and-conversion", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-004, claim: "Integer promotion: типы ранга ниже int продвигаются к int/unsigned int", source: "iso-c11-n1570", section: "6.3.1.1", skill: "c-integer-promotion-and-conversion", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-005, claim: "SysV AMD64: первые 6 целочисленных аргументов в rdi/rsi/rdx/rcx/r8/r9, стек выровнен на 16 байт перед call", source: "sysv-amd64-abi", section: "3.2.1, 3.2.5", skill: "abi-layout-reasoning", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-006, claim: "Red zone (128 байт ниже rsp) не модифицируется сигнальными обработчиками", source: "sysv-amd64-abi", section: "3.2.2", skill: "abi-layout-reasoning", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-007, claim: "AAPCS64: аргументы в x0-x7, результат в x0, indirect result в x8, стек выровнен на 16 байт", source: "aapcs64", section: "6.1-6.3, 7", skill: "abi-layout-reasoning", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-008, claim: "Borrow checker доказывает отсутствие data race только в safe-коде; ничего не доказывает про unsafe/panic/deadlock/memory-ordering/FFI ABI", source: "rust-reference", section: "unsafety.html", skill: "rust-unsafe-reasoning", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-009, claim: "transmute между типами несовместимого размера/выравнивания — UB", source: "rust-reference", section: "behavior-considered-undefined.html", skill: "rust-unsafe-reasoning", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-010, claim: "Data race (два доступа к неатомарному объекту, минимум один — запись, без happens-before) — UB", source: "rust-reference", section: "behavior-considered-undefined.html", skill: "memory-ordering-reasoning", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-011, claim: "Relaxed не даёт ordering между потоками, только атомарность операции", source: "iso-cpp20-n4861", section: "[atomics.order]", skill: "memory-ordering-reasoning", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-012, claim: "Компилятор вправе считать UB недостижимым и удалять/трансформировать код на этом основании", source: "carruth-gigo", section: "whole talk", skill: "compiler-ub-assumptions", confidence: MUST_CONSIDER, evidence: INFERRED}
  - {id: CL-013, claim: "Panic/unwind через extern \"C\" границу — UB", source: "rustonomicon", section: "ffi", skill: "rust-ffi-boundary", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-014, claim: "void* → T* требует выравнивания по T, иначе UB при разыменовании", source: "iso-c11-n1570", section: "6.3.2.3, 6.2.8", skill: "c-undefined-behavior", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-015, claim: "Access к freed memory — UB (UAF)", source: "iso-c11-n1570", section: "7.22.3", skill: "c-undefined-behavior", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-016, claim: "volatile не даёт memory ordering — нужны барьеры/атомики для синхронизации с ISR", source: "arm-arm", section: "memory model", skill: "embedded-volatile-and-memory-ordering", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-017, claim: "Обработчик сигнала может вызывать только async-signal-safe функции (malloc/printf/лок — нельзя)", source: "cert-c", section: "SIG30-C", skill: "c-signal-handler-safety", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-018, claim: "NASM чувствителен к регистру в большинстве символов: Loop, loop и LOOP — разные имена", source: "nasm-manual", section: "section 3.1", skill: "asm-syntax-dialects-nasm-gas-att", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-019, claim: "В Intel-синтаксисе mov rax,buf кладёт АДРЕС buf, а mov rax,[buf] — СОДЕРЖИМОЕ; отсутствие скобок молча меняет семантику", source: "nasm-manual", section: "section 3.3", skill: "asm-syntax-dialects-nasm-gas-att", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-020, claim: "Thumb-2 cbz/cbnz кодируют только low-регистры r0-r7; использование r8-r12 невалидно", source: "arm-arm", section: "Thumb-2 branch instructions", skill: "asm-arm-thumb-2-encoding", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-021, claim: "Повторное использование nonce с тем же ключом в AES-GCM фатально: вскрывается аутентичность и конфиденциальность", source: "nist-sp800-38d", section: "section 8 (IV/nonce uniqueness)", skill: "rust-crypto-primitives-safety", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-022, claim: "RISC-V calling convention: аргументы в a0-a7, возврат в a0, ra — return address, s0-s11 callee-saved, стек выровнен на 16", source: "riscv-psabi", section: "riscv-cc.adoc", skill: "asm-risc-v-registers-and-calling-conventions", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-023, claim: "AT&T-синтаксис: операнды source,dest (movl src,dst); Intel-синтаксис: dest,src (mov dst,src) — инверсия", source: "binutils-docs", section: "GNU as i386 syntax", skill: "asm-syntax-dialects-nasm-gas-att", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-024, claim: "Zig comptime выполняется на этапе компиляции; @typeInfo/@Type позволяют интроспекцию типов, comptime for разворачивается до генерации кода", source: "zig-langref", section: "Comptime", skill: "zig-comptime-metaprogramming", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-025, claim: "В Zig аллокаторы явные: нет скрытой аллокации; GeneralPurposeAllocator в Debug/ReleaseSafe ловит утечки и double-free", source: "zig-std-source", section: "std/heap.zig", skill: "zig-allocators-and-memory-management", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-026, claim: "LLM галлюцинируют несуществующие крейты, похожие на реальные (опечатка/дефис), с устойчивой между моделями частотой", source: "arxiv-2606-08444", section: "abstract", skill: "rust-dependency-supply-chain", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-027, claim: "VM-утечка: страница пула переиспользуется без munmap — RSS растёт, хотя heap-аллокатор чист", source: "ghostty-memory-leak", section: "whole post", skill: "binary-memory-leak-vm-allocator-diagnosis", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-028, claim: "Оракул allclose на фиксированном shape сертифицирует бажные GPU-ядра как корректные; нужен fuzz+fp64-референс", source: "arxiv-2606-20128", section: "abstract", skill: "gpu-kernel-verification-beyond-oracle", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-029, claim: "Дизассемблер LLM: asm->IR exact match всего 14% (round-trip 45%) — транспозиции почти всегда неверны", source: "arxiv-2407-02524", section: "abstract", skill: "binary-disassembly-decompilation-fidelity", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-030, claim: "compile@k не равен pass@k: «компилируется» не значит «верно», cliff на ~200 инструкциях", source: "arxiv-2607-06125", section: "abstract", skill: "binary-disassembly-decompilation-fidelity", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-031, claim: "CFS заменён на EEVDF (lag-based) в ядре 6.6; vruntime/lag определяют выбор задачи", source: "kernel-docs-sched", section: "sched-eevdf.rst", skill: "kernel-scheduler-mm-vfs-internals", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-032, claim: "file_operations-колбэки исполняются в контексте процесса; copy_from_user проверяется на частичное копирование", source: "kernel-driver-api", section: "char drivers", skill: "kernel-driver-char-device-lifecycle", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-033, claim: "CMake-зависимости объявляются через target_link_libraries + find_package, а не угадыванием версий в if()", source: "cmake-docs", section: "cmake-buildsystem", skill: "build-system-cmake-diagnostics", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-034, claim: "undefined reference может означать ABI/версионное несовпадение, а не отсутствующую функцию; проверяй nm/readelf символы", source: "lld-docs", section: "undefined symbols", skill: "build-linker-error-diagnostics", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-035, claim: "Miri исполняет MIR и детектирует UB (включая UAF и нарушение aliasing) в unsafe-коде", source: "rust-miri", section: "README", skill: "rust-unsafe-safety-contract-verification", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-036, claim: "SAFETY-комментарий обязан ссылаться на реальные инварианты, закодированные в типах/lifetimes; «caller guarantees» без PhantomData/lifetime не существует", source: "rustonomicon", section: "meet-safe-and-unsafe", skill: "rust-unsafe-safety-contract-verification", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-037, claim: "Сравнение строк с early-exit по длине утекает длину префикса (тайминг); нужен constant-time memcmp (CWE-1254)", source: "cwe-1254", section: "description", skill: "side-channel-constant-time-verification", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-038, claim: "Z3-unsat доказывает противоречивость ТОЛЬКО данных аксиом; «prover passed» ≠ верно, если аксиомы неверны", source: "z3-docs", section: "Solver", skill: "smt-z3-sound-usage", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-039, claim: "Инвариант цикла должен быть индуктивным и влечь постусловие; слишком слабый/vacuous инвариант «проходит» проверку, но ничего не доказывает", source: "acsl-spec", section: "loop invariant", skill: "formal-spec-loop-invariants", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-040, claim: "compatible-строка devicetree обязана совпадать с binding; node-адреса уникальны в рамках parent согласно #address-cells", source: "devicetree-spec", section: "node properties", skill: "embedded-device-tree-and-kconfig", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-041, claim: "Имена/биты регистров берутся из datasheet, а не из памяти модели; reserved-биты только читаются", source: "stm32-ref-manual", section: "GPIO/I2C registers", skill: "embedded-hw-register-datasheet-verification", confidence: MUST_VERIFY, evidence: KNOWN}
  - {id: CL-042, claim: "VT-x: VMXON выполняется до VM entry; причина VMEXIT читается из VMCS", source: "intel-sdm", section: "Vol.3C ch 24-25", skill: "hypervisor-vmx-svm-internals", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-043, claim: "Блокирующие MPI_Send/Recv в неверном порядке дают deadlock; non-blocking + Waitall устраняет", source: "mpi-41", section: "ch 3 point-to-point", skill: "hpc-mpi-programming", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-044, claim: "Гонка на shared-переменных OpenMP-цикла без reduction; reduction-клауза даёт приватную копию на поток", source: "openmp-spec", section: "worksharing", skill: "hpc-openmp-parallel-programming", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-045, claim: "CDC: однобитные сигналы требуют 2-FF синхронизатора, многобитные — handshake/async FIFO; constraint не чинит CDC-bug", source: "cummings-cdc", section: "CDC papers", skill: "hdl-cdc-audit", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-046, claim: "DEX-инструкции оперируют виртуальными регистрами (v0/p0); dispatch через invoke-virtual", source: "dex-spec", section: "instruction set", skill: "android-re-dex-smali-format", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-047, claim: "JNI local-ссылки недействительны после возврата из native-метода; нужен NewGlobalRef для сохранения", source: "jni-spec", section: "references", skill: "android-re-native-jni-analysis", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-048, claim: "UEFI Boot Services действительны только до ExitBootServices(); после — только Runtime Services", source: "uefi-spec", section: "boot services", skill: "bootloader-uefi-firmware", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-049, claim: "Заявление о CVE без воспроизводимого sanitizer-отчёта + минимизированного инпута + достижимого пути недопустимо", source: "aflpp-docs", section: "crash minimization", skill: "fuzzing-harness-evidence-gate", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-050, claim: "«Фейковый параллелизм»: thread-safe примитивы (ConcurrentHashMap/atomic) при фактически однопоточном исполнении не дают параллельности", source: "arxiv-2603-03683", section: "abstract", skill: "concurrency-actual-parallelism-detection", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-051, claim: "Поведенческие изменения API Rust (та же сигнатура, другая семантика) модели решают 38% против 65.8% на стабилизированных", source: "arxiv-2503-16922", section: "abstract", skill: "rust-api-evolution-and-drift", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-052, claim: "Номера syscall'ов и константы shellcode читаются из таблицы/байтов, а не угадываются по памяти", source: "linux-syscall-table", section: "syscall_64.tbl", skill: "reverse-engineering-shellcode-analysis", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-053, claim: "Zig кросскомпиляция задаётся target triple через zig cc -target / build -Dtarget; std.Target.Query описывает", source: "zig-langref", section: "Targets", skill: "zig-cross-compilation-targets", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-054, claim: "Zig build-система: build.zig описывает шаги, зависимости пакетов — в build.zig.zon", source: "zig-build-guide", section: "packages", skill: "zig-build-system-and-packages", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-055, claim: "Zig error union error{T}!T с try/catch; defer/errdefer исполняются в обратном порядке (LIFO)", source: "zig-langref", section: "Errors/Defer", skill: "zig-error-model-and-defers", confidence: MUST_CONSIDER, evidence: KNOWN}
  - {id: CL-056, claim: "KVM-модель: KVM_CREATE_VM создаёт VM, KVM_CREATE_VCPU — vCPU, KVM_RUN запускает гость до VMEXIT", source: "kvm-docs", section: "ioctls", skill: "hypervisor-vmx-svm-internals", confidence: MUST_CONSIDER, evidence: KNOWN}

# ============================================================
# Provenance verification (автоматизируемая)
# ============================================================
provenance_checks:
  - "Каждый claim в stable skill должен иметь непустой source и section"
  - "source id должен существовать в registry/sources.yaml"
  - "section должен быть реальным (не выдуманным)"
  - "evidence: KNOWN только если источник нормативный/официальный"
  - "UNVERIFIED claims запрещены в stable skill — только в draft/researched"
  - "Различать: direct quotation (цитата) vs paraphrase (пересказ) vs inspiration (идея) — хранить в provenance"
